#define foreach(v, i) for(int i = 0; i < v.size(); ++i)

#define io(a) cin >> a

#define FUNCTION(a, b)

#define maximum(m, x) \
    if (m < x) m = x

#define minimum(m, x) \
    if (m > x) m = x

#define INF 1000000000

#define toStr(a) #a
